package org.densyakun.peoplelist.common;

public class PersonMetadataKeys {
	
	public static final String address = "addr";
	public static final String postal_code = "postcode";
	public static final String email = "email";
	public static final String phone_number = "phone_number";
	public static final String office_number = "office_number";
	
	/*public static String getTranslated(Language language, String key) {
		if (key.equals(address)) {
			
		} else if (key.equals(postal_code)) {
			
		} else if (key.equals(email)) {
			
		} else if (key.equals(phone_number)) {
			
		} else if (key.equals(office_number)) {
			
		}
		return "";
	}*/
	
}
